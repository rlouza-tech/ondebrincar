# S2.1 / I1.1 — Rotas base com dados mockados

**Data:** 2026-05-15  
**Story:** US-I1.1 (fase A)

## Decisões

| Tema | Escolha | Motivo |
|------|---------|--------|
| Home | SSG (`getAllAtracoes` em build) | Lista estável no MVP; HTML estático na CDN |
| Ficha `/atracao/[slug]` | SSG + `generateStaticParams` | URLs previsíveis e SEO; slugs finitos no mock |
| Busca `/buscar` | Página estática + filtro **client-side** (`useSearchParams`) | `searchParams` sem forçar SSR dinâmico; suficiente pra fase A |
| Dados | `lib/mock-atracoes.ts` | Substituído por Sanity em **US-I2.1** |
| URL da ficha | `/atracao/[slug]` (singular) | Uma atração por URL; alinhado ao data-model (`slug` único) |

## Rotas

| Rota | Renderização | Metadata |
|------|----------------|----------|
| `/` | SSG | `generateMetadata` estático |
| `/atracao/[slug]` | SSG | `generateMetadata` por slug |
| `/buscar` | Static shell + client filter | `generateMetadata` estático |

## Fora de escopo (fase A)

- Integração Sanity (US-I2.1)
- Formulário de busca com submit (filtro só via query string)
- Favoritos funcionais nos cards
