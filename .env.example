# Copie este arquivo para .env.local e preencha os valores reais.
# Nunca commite .env.local nem tokens reais no repositório.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=
GEMINI_API_KEY=
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_CLARITY_ID=
NEXT_PUBLIC_BEEHIIV_FORM_ID=
RAINDROP_API_TOKEN=
# US-I51 — protege /api/cron/destaques. A Vercel envia esse valor automaticamente
# como "Authorization: Bearer <CRON_SECRET>" quando o cron está configurado em produção.
CRON_SECRET=
